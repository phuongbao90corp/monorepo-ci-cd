// Simple utility function to test
export function formatThemeColor(color: string): string {
  return color.toLowerCase().trim();
}

export function add(a: number, b: number): number {
  return a + b;
}
